import React, { useState, useEffect } from 'react';
import { 
  LineChart, Activity, Terminal, Settings, Play, Square, 
  Cpu, HardDrive, Wifi, Bitcoin, Zap, Layers 
} from 'lucide-react';

// Mock Data para las 3 estrategias Cripto
const initialStrategies = [
  { id: 1, name: "EMA Trend Crossover", type: "Tendencia", status: "running", pnl: 145.20, trades: 12, asset: "BTC/USD", icon: <Bitcoin size={16} /> },
  { id: 2, name: "Bollinger Volatility Breakout", type: "Ruptura", status: "running", pnl: -24.50, trades: 8, asset: "ETH/USD", icon: <Zap size={16} /> },
  { id: 3, name: "Dynamic Spot Grid", type: "Market Making", status: "running", pnl: 56.80, trades: 142, asset: "SOL/USD", icon: <Layers size={16} /> }
];

const mockLogs = [
  "[02:14:01] INFO [Crypto_Engine]: Conexión WS Alpaca Cripto (wss://stream.data.alpaca.markets/v1beta3/crypto/us) establecida.",
  "[02:14:05] INFO [Grid_Bot]: Rango actualizado para SOL/USD. Colocando 5 bids y 5 asks.",
  "[02:15:22] EXEC [Grid_Bot]: Orden LIMIT FILL -> COMPRA 2.5 SOL @ 142.50",
  "[02:20:00] INFO [EMA_Bot]: Escaneando BTC/USD... EMA12 (68,450) < EMA26 (68,600). Tendencia bajista, esperando.",
  "[02:35:10] WARN [BB_Bot]: Expansión de bandas detectada en ETH/USD con pico de volumen. Calculando sizing.",
  "[02:35:12] EXEC [BB_Bot]: Orden MARKET enviada -> COMPRA 0.45 ETH @ 3,450.25"
];

export default function CryptoDashboard() {
  const [strategies, setStrategies] = useState(initialStrategies);
  const [logs, setLogs] = useState(mockLogs);
  const [activeTab, setActiveTab] = useState('dashboard');

  return (
    <div className="min-h-screen bg-[#0a0e17] text-slate-300 font-sans flex overflow-hidden">
      
      {/* Sidebar */}
      <aside className="w-64 border-r border-slate-800 bg-[#0f172a]/80 flex flex-col justify-between">
        <div>
          <div className="p-6 border-b border-slate-800 flex items-center gap-3">
            <Bitcoin className="text-amber-500" size={24} />
            <h1 className="text-xl font-bold text-slate-100">Alpaca<span className="text-amber-500">Crypto</span></h1>
          </div>
          <nav className="p-4 space-y-2">
            <NavItem icon={<LineChart />} label="Dashboard" active={activeTab === 'dashboard'} onClick={() => setActiveTab('dashboard')} />
            <NavItem icon={<Terminal />} label="System Logs" active={activeTab === 'logs'} onClick={() => setActiveTab('logs')} />
            <NavItem icon={<Settings />} label="Configuración" active={activeTab === 'settings'} onClick={() => setActiveTab('settings')} />
          </nav>
        </div>
        
        {/* VPS Status Widget */}
        <div className="p-4 m-4 bg-slate-800/40 rounded-xl border border-slate-700/50">
          <h3 className="text-xs font-semibold text-slate-400 uppercase tracking-wider mb-3">VPS Status (Easypanel)</h3>
          <div className="space-y-3 text-sm">
            <div className="flex justify-between items-center">
              <span className="flex items-center gap-2"><Cpu size={14} className="text-slate-400"/> CPU</span>
              <span className="text-emerald-400">8%</span>
            </div>
            <div className="flex justify-between items-center">
              <span className="flex items-center gap-2"><HardDrive size={14} className="text-slate-400"/> RAM</span>
              <span className="text-amber-400">280 MB</span>
            </div>
            <div className="flex justify-between items-center">
              <span className="flex items-center gap-2"><Wifi size={14} className="text-slate-400"/> Latencia WS</span>
              <span className="text-emerald-400">18ms</span>
            </div>
          </div>
        </div>
      </aside>

      {/* Main Content */}
      <main className="flex-1 flex flex-col h-screen overflow-hidden">
        
        {/* Header */}
        <header className="h-16 border-b border-slate-800 bg-[#0f172a]/50 flex items-center justify-between px-8">
          <h2 className="text-lg font-medium text-slate-100">
            {activeTab === 'dashboard' && 'Panel de Control Cripto - Operativa 24/7'}
            {activeTab === 'logs' && 'Registro de Eventos (Logs en Tiempo Real)'}
            {activeTab === 'settings' && 'Configuración de API & Variables de Entorno'}
          </h2>
          <div className="flex items-center gap-3">
            <span className="flex items-center gap-2 text-sm bg-slate-800/80 px-3 py-1 rounded-full border border-slate-700 text-slate-300">
              Uptime: <span className="text-amber-400 font-mono">14d 08h 22m</span>
            </span>
            <span className="flex items-center gap-2 text-sm bg-amber-500/10 px-3 py-1 rounded-full border border-amber-500/30 text-amber-400">
              <span className="w-2 h-2 rounded-full bg-amber-500 animate-pulse"></span>
              Alpaca Crypto Stream
            </span>
          </div>
        </header>

        {/* Content Area */}
        <div className="flex-1 overflow-auto p-8">
          
          {activeTab === 'dashboard' && (
            <div className="space-y-6">
              {/* Resumen Global */}
              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                <StatCard title="Capital Total (USD)" value="$15,177.50" change="+1.18%" isPositive={true} />
                <div className="bg-slate-800/40 border border-slate-700/50 rounded-2xl p-6 flex items-center justify-between">
                  <div>
                    <h3 className="text-slate-400 text-sm font-medium">Estado del Mercado</h3>
                    <p className="text-xl font-bold text-emerald-400 mt-2">Abierto 24/7</p>
                  </div>
                  <div className="w-12 h-12 rounded-full bg-amber-500/10 flex items-center justify-center text-amber-500">
                    <Activity size={24} />
                  </div>
                </div>
                <StatCard title="Trades 24H" value="162" change="Volatilidad Alta" isPositive={true} />
              </div>

              {/* Lista de Estrategias Cripto */}
              <div>
                <h3 className="text-lg font-medium text-slate-100 mb-4">Monitor de Algoritmos Cripto</h3>
                <div className="grid grid-cols-1 xl:grid-cols-3 gap-6">
                  {strategies.map(s => (
                    <StrategyCard key={s.id} strategy={s} />
                  ))}
                </div>
              </div>
            </div>
          )}

          {activeTab === 'logs' && (
            <div className="h-full bg-[#05080f] rounded-lg border border-slate-800 p-4 font-mono text-sm overflow-auto shadow-inner relative">
              <div className="absolute top-2 right-2 flex gap-2">
                <button className="text-xs bg-slate-800 hover:bg-slate-700 text-slate-300 px-3 py-1 rounded border border-slate-700 transition">Descargar .log</button>
              </div>
              {logs.map((log, i) => (
                <div key={i} className={`mb-1 ${log.includes('WARN') ? 'text-amber-400' : log.includes('EXEC') ? 'text-emerald-400' : 'text-slate-400'}`}>
                  {log}
                </div>
              ))}
              <div className="animate-pulse text-slate-500 mt-2">_</div>
            </div>
          )}

        </div>
      </main>
    </div>
  );
}

// Subcomponentes
function NavItem({ icon, label, active, onClick }) {
  return (
    <button 
      onClick={onClick}
      className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200 ${
        active ? 'bg-slate-800/80 text-amber-400 shadow-lg border border-amber-500/20' : 'text-slate-400 hover:bg-slate-800 hover:text-slate-200'
      }`}
    >
      {icon}
      <span className="font-medium">{label}</span>
    </button>
  );
}

function StatCard({ title, value, change, isPositive }) {
  return (
    <div className="bg-slate-800/40 border border-slate-700/50 rounded-2xl p-6">
      <h3 className="text-slate-400 text-sm font-medium">{title}</h3>
      <div className="mt-2 flex items-baseline gap-4">
        <span className="text-3xl font-bold text-slate-100">{value}</span>
        <span className={`text-sm font-medium ${isPositive ? 'text-emerald-400' : 'text-rose-400'}`}>
          {change}
        </span>
      </div>
    </div>
  );
}

function StrategyCard({ strategy }) {
  const statusColors = {
    running: 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20',
    paused: 'bg-amber-500/10 text-amber-400 border-amber-500/20',
  };

  return (
    <div className="bg-[#121b2b] border border-slate-700/50 rounded-xl p-6 hover:border-slate-600 transition-colors">
      <div className="flex justify-between items-start mb-4">
        <div>
          <div className="flex items-center gap-2 mb-1">
            <span className="text-slate-400">{strategy.icon}</span>
            <h4 className="text-slate-100 font-semibold">{strategy.name}</h4>
          </div>
          <span className="text-xs font-mono text-amber-400/80 bg-amber-500/10 px-2 py-0.5 rounded mt-1 inline-block border border-amber-500/20">
            {strategy.asset}
          </span>
        </div>
        <div className={`px-2.5 py-1 rounded-full text-xs font-medium border flex items-center gap-1.5 ${statusColors[strategy.status]}`}>
          {strategy.status === 'running' && <Play size={10} />}
          {strategy.status.toUpperCase()}
        </div>
      </div>
      
      <div className="grid grid-cols-2 gap-4 mt-6 pt-4 border-t border-slate-800">
        <div>
          <p className="text-xs text-slate-500 mb-1">PnL Realizado</p>
          <p className={`text-xl font-mono font-medium ${strategy.pnl >= 0 ? 'text-emerald-400' : 'text-rose-400'}`}>
            {strategy.pnl >= 0 ? '+' : ''}${strategy.pnl.toFixed(2)}
          </p>
        </div>
        <div>
          <p className="text-xs text-slate-500 mb-1">Volumen Trades</p>
          <p className="text-xl font-mono text-slate-200">{strategy.trades}</p>
        </div>
      </div>
    </div>
  );
}